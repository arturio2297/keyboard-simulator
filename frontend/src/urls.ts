const main = '/';

const login = '/sing-in';

const registration = '/sing-up'

const workout = '/workout';

const about = '/about';

const urls = {
  main,
  login,
  registration,
  workout,
  about
};

export default urls;