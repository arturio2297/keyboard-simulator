const main = '/';

const login = '/sing-in';

const registration = '/sing-up'

const workout = '/workout';

const about = '/about';

const profile = '/profile'

const passwordRecovery = '/password-recovery';

const urls = {
  main,
  login,
  registration,
  workout,
  about,
  profile,
  passwordRecovery
};

export default urls;