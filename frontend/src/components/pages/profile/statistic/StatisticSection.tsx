import {observer} from "mobx-react-lite";
import styles from "./styles.module.css";

function StatisticSection(): JSX.Element {

  return (
    <section className={styles['statistic-section']}>

    </section>
  );
}

export default observer(StatisticSection);