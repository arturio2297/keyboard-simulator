import {observer} from "mobx-react-lite";
import styles from "../styles.module.css";

function SendCodeSection(): JSX.Element {

  return (
    <section className={styles['send-code-section']}>

    </section>
  );
}

export default observer(SendCodeSection);